logLevel: info
logDestinations: [stdout]
rtspAddress: :8554
hlsAddress: :8888

paths:
  # Primary test stream — used by seed-dev camera
  test:
    source: /test-media/test.mp4
    sourceOnDemand: false
    loop: true

  # Additional streams for multi-camera Phase 2 testing
  # All three loop the same synthetic video; add different videos when available
  test2:
    source: /test-media/test.mp4
    sourceOnDemand: true
    loop: true

  test3:
    source: /test-media/test.mp4
    sourceOnDemand: true
    loop: true
